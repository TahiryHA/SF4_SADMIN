<!-- Navbar -->
<nav
	class="main-header navbar navbar-expand navbar-light navbar-dark">
	<!-- Left navbar links -->
	<ul class="navbar-nav">
		<li class="nav-item">
			<a class="nav-link" data-widget="pushmenu" href="#" role="button">
				<i class="fas fa-bars"></i>
			</a>
		</li>
	</ul>
	<!-- Right navbar links -->
	<ul class="navbar-nav ml-auto">
		<li class="nav-item dropdown user-menu">
			<a
				href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="{{app.user.id}}">
				{% if (app.user.image == "https://cdn4.iconfinder.com/data/icons/e-commerce-icon-set/48/Username_2-512.png") %}
				                <img src="{{asset('Username_2-512.webp')}}" id="userProfileImage" class="user-image img-circle elevation-2"
				                    alt="User Image">
				                {% else %}
				                <img src="{{asset("uploads/profile/") ~ app.user.image}}" id="userProfileImage"
				                    class="user-image img-circle elevation-2" alt="User Image">
				                {% endif %}
				<span id="username" class="d-none d-md-inline">{{app.user.username|capitalize}}</span>
			</a>
			<div class="dropdown-menu dropdown-menu-right" style="width: 140px;">
				<span class="dropdown-item dropdown-header user-header">{{app.user.username|capitalize}}
					<br>
					{{app.user.email}}</span>
				<div class="dropdown-divider"></div>
				{% if is_granted('ROLE_SUPER_ADMIN') or is_granted('ROLE_ADMIN') %}
					<a class="dropdown-item btnEditProfileUser" href="javascript:void(0)" id="{{app.user.id}}" role="menuitem">
						<i class="nav-icon fas fa-user mr-2" aria-hidden="true"></i>
						Profile</a>
					<div class="dropdown-divider"></div>
				{% endif %}
					<a class="dropdown-item btnChangePasswordUser" href="javascript:void(0)" id="{{app.user.id}}" role="menuitem">
						<i class="nav-icon fas fa-lock mr-2" aria-hidden="true"></i>
						Mot de passe</a>
					<div class="dropdown-divider"></div>
				<a class="dropdown-item" href="{{path("app.logout")}}" role="menuitem">
					<i class="nav-icon fas fa-power-off mr-2" aria-hidden="true"></i>
					Déconnexion</a>
				<div class="dropdown-divider"></div>
			</div>
		</li>
	</ul>
</nav>
<!-- /.navbar -->
